import 'package:flutter/cupertino.dart';

class WrapCardWidget extends Container {
  var position = -1;

  WrapCardWidget({required Container child}) : super(child: child);

  @override
  Widget build(BuildContext context) {
    return super.build(context);
  }
}
